module NN
    ( 
    ) where
import Control.Monad
import Data.Functor
import Data.List
--import System.Random

{-
gauss :: Float -> IO Float
gauss stdev = do
  x<-randomIO
  y<-randomIO
  return $stdev*sqrt(-2*log x)*cos(2*pi*y)
-}
